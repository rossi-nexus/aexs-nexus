# Frontend Supabase config — points at the AEXS-owned nexus-production project.
# Created 2026-06-18 as the migration destination (Lovable → Vercel).
# The earlier æXs NEXUS project (gnenmdrvkfzgnsiheqcj) is preserved as an archive
# of a prior NEXUS iteration and is NOT the target of this migration.
#
# For local dev, copy this to .env.
VITE_SUPABASE_PROJECT_ID="plnwneqqbmtwoaaeirta"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_iRVZPhnQ3M0rco6_76MMTQ_nw4maye0"
VITE_SUPABASE_URL="https://plnwneqqbmtwoaaeirta.supabase.co"

# Set to "true" in dev to bypass the auth gate. Banner will appear when active.
# Production builds ignore this flag unconditionally.
VITE_ALLOW_AUTH_BYPASS=
